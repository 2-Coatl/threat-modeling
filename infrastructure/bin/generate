#!/usr/bin/env bash
# Wrapper preserved for backward compatibility; delegates to top-level bin/generate

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec "${PROJECT_ROOT}/bin/generate" "$@"
