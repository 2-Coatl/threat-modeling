#!/usr/bin/env bash
# Project-level wrapper for infrastructure/bin/generate

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec "${PROJECT_ROOT}/infrastructure/bin/generate" "$@"
